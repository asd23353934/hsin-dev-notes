# Go Stack

> 事實型紀錄：Go 版本、模組、執行環境。
> **角色**：新專案起手套餐 / 預設假設。實際專案以該專案 `go.mod` 為準。
> 對應作品：`noteGolang`。

---

## 待補

此資料夾目前為**結構佔位**。Go 的版本與常用套件**待實際專案開工時，依 [_global/rules.md](../_global/rules.md) §7 驗證後填入**（Go 版本、router / ORM 選型、CLI / worker 慣用庫等）。

慣例（專案佈局、error handling、`context` 傳遞、介面設計、table-driven test）與踩坑待實際遇到再建 `conventions.md` / `errors.md`，套用 [CLAUDE.md](../CLAUDE.md) 的模板。
